# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit secrets
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# OMC（オーケストレーション層）のセッション状態 — リポに含めない
.omc/
.claude/

# ローカル作業用（キャラ生成・デモ）
.kaizen-chara/
.widget-demo/
.claude/
.env*
# 本番envのローカルダンプ（vercel env pull 等の一時ファイル）。
# `.env*` は "env-prod.tmp" のような env で始まらない名前に当たらないため、明示的に塞ぐ。
# 2026-08-12：本番シークレット3KBの平文ダンプが untracked のまま放置されていた実例あり。
*env*.tmp
env-prod*
